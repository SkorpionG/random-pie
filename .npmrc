# NPM Configuration for random-pie package

# Use npm registry
registry=https://registry.npmjs.org/

# Save exact versions in package.json
save-exact=true

# Engine strict - enforce Node version requirements
engine-strict=true
